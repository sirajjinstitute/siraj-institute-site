/* Google Analytics 4 bootstrap. Moved verbatim out of index.html (Wave 1B)
   so that no executable JavaScript remains inline in the document.
   Loads synchronously in <head>, in the same position and order as the
   inline block it replaces, so gtag() is defined before any use. */
window.dataLayer = window.dataLayer || [];
function gtag(){dataLayer.push(arguments);}
gtag('js', new Date());
gtag('config', 'G-667Q0LLEH2');
